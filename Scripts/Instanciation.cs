/* 
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Instanciation : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
		foreach (var Item in Data.Instance.purchases.Count);
		print(Data.Instance.purchases.Count);
	}
	
	Instantiate(item);
	print(item);

}
*/